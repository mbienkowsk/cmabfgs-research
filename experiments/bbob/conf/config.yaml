defaults:
  - optimizers/cmaes@optimizers.cmaes
  - optimizers/cmabfgs@optimizers.cmabfgs
  - _self_

n_batches: 10
batch: 0
dimensions: 2
seed: 17
optimizer: cmaes
popsize: "hansen"
stopping_criterion: "unlimited"

hydra:
  sweeper:
    params:
      batch: range(0,${n_batches})
      dimensions: 2,3,5
