defaults:
  - launcher: local
  - override hydra/launcher: submitit_local
  - _self_

n_batches: 10
batch: 0
dimensions: 2
seed: 17

hydra:
  sweeper:
    params:
      batch: range(0,${n_batches})
      dimensions: 2,3,5
